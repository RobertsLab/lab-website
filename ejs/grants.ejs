
```{=html}
<ul class="grant-list list">
<% for (const item of items) { %>
  <li <%= metadataAttrs(item) %>>
    <span class="listing-title lead"><%= item.title %></span><br />

    <a href="<%- item.path %>" class="btn btn-primary">
      Summary
    </a>
    <% if (item.website) { %>
    <a href="<%- item.website %>" class="btn btn-secondary" target="_blank" rel="noopener noreferrer">
      Website
    </a>
    <% } %>
    <% if (item.preprint) { %>
    <a href="<%- item.preprint %>" class="btn btn-light" target="_blank" rel="noopener noreferrer">
      <i class="bi bi-file-earmark-text"></i> Preprint
    </a>
    <% } %>
    <% if (item.github) { %>
    <a href="<%- item.github %>" class="btn btn-light" target="_blank" rel="noopener noreferrer">
      <i class="bi bi-file-earmark-zip"></i> GitHub Repository
    </a>
    <% } %>
<strong>Support</strong>::<span class="listing-agency"><%= item.agency %></span>

<br>
<br>

  </li>
<% } %>
</ul>

```
